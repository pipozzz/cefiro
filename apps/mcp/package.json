{
  "name": "@norish/mcp",
  "version": "0.24.0-beta",
  "private": true,
  "type": "module",
  "description": "MCP server for Cefiro — create and search recipes over the public API from any MCP client.",
  "bin": {
    "cefiro-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
