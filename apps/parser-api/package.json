{
  "name": "@norish/parser-api",
  "version": "0.24.0-beta",
  "private": true,
  "scripts": {
    "postinstall": "sh -c 'if [ \"${SKIP_PARSER_API_INSTALL:-0}\" = \"1\" ]; then exit 0; fi; if ! command -v uv >/dev/null 2>&1; then printf \"Skipping parser-api dependency install: uv not found\\n\"; exit 0; fi; uv sync --locked'",
    "install:deps": "uv sync --locked",
    "dev": "sh -c 'uv run --locked uvicorn app.main:app --app-dir . --reload --host 127.0.0.1 --port ${PARSER_API_PORT:-8001}'"
  }
}
