{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".github/workflows/**", "pnpm-workspace.yaml"],
  "globalPassThroughEnv": [
    "NODE_ENV",
    "CI",
    "npm_lifecycle_event",
    "SKIP_ENV_VALIDATION",
    "DATABASE_URL",
    "MASTER_KEY",
    "AUTH_URL",
    "OBSCURA_ENDPOINT",
    "REDIS_URL",
    "PARSER_API_TIMEOUT_MS"
  ],
  "tasks": {
    "topo": {
      "dependsOn": ["^topo"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "dist-server/**",
        ".cache/**",
        "**/*.tsbuildinfo",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "build:server": {
      "dependsOn": ["^build:server"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["^dev"],
      "cache": false,
      "persistent": true
    },
    "lint:check": {
      "dependsOn": ["^lint:check"],
      "outputs": []
    },
    "lint": {
      "dependsOn": [],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": ["dist/**", "**/*.tsbuildinfo", ".cache/**"]
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    },
    "format": {
      "dependsOn": ["^format"],
      "outputs": [],
      "outputLogs": "new-only"
    },
    "deps:cycles": {
      "cache": false
    }
  }
}
